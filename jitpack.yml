jdk:
  - openjdk11

install:
  - chmod +x gradlew
  # Build core module (produces .jar)
  - ./gradlew :core:build -x test
  # Build android module (produces .aar)
  - ./gradlew :android:assembleRelease -x test