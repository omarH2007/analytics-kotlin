jdk:
  - openjdk11
before_install:
  - chmod +x gradlew
env:
  - JITPACK=true
install:
  - ./gradlew :core:publishToMavenLocal :android:publishToMavenLocal -Prelease -x test -x signReleasePublication

